[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn main:app --host :: --port ${PORT:-8001}"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
port = 8001
